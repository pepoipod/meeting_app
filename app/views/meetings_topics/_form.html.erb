<%= form_for(@meetings_topic) do |f| %>
  <% if @meetings_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meetings_topic.errors.count, "error") %> prohibited this meetings_topic from being saved:</h2>

      <ul>
      <% @meetings_topic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :meeting_id_id %><br>
    <%= f.text_field :meeting_id_id %>
  </div>
  <div class="field">
    <%= f.label :topic_id_id %><br>
    <%= f.text_field :topic_id_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
