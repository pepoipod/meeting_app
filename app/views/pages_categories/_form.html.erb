<%= form_for(@pages_category) do |f| %>
  <% if @pages_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pages_category.errors.count, "error") %> prohibited this pages_category from being saved:</h2>

      <ul>
      <% @pages_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :page_id %><br>
    <%= f.text_field :page_id %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br>
    <%= f.text_field :category_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
